#include <stdio.h>
#include <conio.h>
#include <math.h>

gloat main()
{
	float a, b, everage;
	printf("Enter two numbers a and b \n");
	scanf("%f%f", &a, &b);
	everage = sqrt(a*b);
	printf("Everage = %g \n", everage);
	getch();
	return 0;
}
