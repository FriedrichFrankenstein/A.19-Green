#include <stdio.h>
#include <conio.h>
#include <math.h>
#include <stdlib.h>
#include <time.h>

int main()
{
    srand ( time ( NULL ) );
    int lengthArray, i, sum = 0;
    printf ( "Enter array length: " );
    scanf ( "%i", &lengthArray );

    getch ();
    return 0;
}
