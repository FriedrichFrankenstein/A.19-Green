#include <stdio.h>
#include <conio.h>
#include <math.h>
#include <time.h>
#include <stdlib.h>

typedef struct {
    int num[10];
} Array;

Array init (Array);

void add (int number);

int main ()
{
    Array array = init(array);
    init(array);
    add(10);
    getch();
    return 0;
}

Array init (Array array){


}
void add (int number){

}
