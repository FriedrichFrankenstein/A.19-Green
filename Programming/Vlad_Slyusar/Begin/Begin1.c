#include <stdio.h>

main ()
{	
	//Виділити пам'ять для вхідних, вихдіних, проміжних даних
	float side, Perim;
	//Ввести вхідні дані
	printf ("Input side (integer!!!) = ");
	scanf ("%f", &side);
	//Обробка даних
	Perim=side*4;
	//Виведення результатів
	printf ("A=%g\nP=4*A=4*%g=%g", side, side, Perim);
	/* %g - працює для комірок типу флоат, але
	на екран виводить без нулів після коми, якщо число було цілим (використовуємо лише для виводу на екран)*/
	getch();
}
